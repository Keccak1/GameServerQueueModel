export const apiAddress = "http://127.0.0.1:5000/api/v1";

const apiAddresses = {
    addPlayerUrl: `${apiAddress}/players`,
    removePlayerUrl: `${apiAddress}/players`,
    addRoomUrl: `${apiAddress}/rooms`,
    status: `${apiAddress}/status`
}

export default apiAddresses;